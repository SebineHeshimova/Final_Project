@model Feedback
@{
    ViewData["Title"] = "Delete";
}

<h1 style="color:black">Delete</h1>
<div>
    <div class="card" style="width: 18rem;">
        <form method="post" enctype="multipart/form-data">
            <img src="~/uploads/feedbacks/@Model.ImageUrl" class="card-img-top" alt="...">
            <div class="card-body">
                <h5 class="card-title">@Model.FullName</h5>
                <h5 class="card-text">@Model.Date</h5>
                <a asp-action="index" asp-controller="feedback" class="btn btn-primary">Go to back</a>
                <button type="submit" class="btn btn-danger">Delete</button>
            </div>
        </form>
    </div>
</div>